@model Proyecto.Model.Clases.Proveedores

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5 d-flex justify-content-center">
    <div class="card shadow-sm bg-light">
        <div class="card-header">
            <h4>Editar Proveedor</h4>
        </div>
        <div class="card-body">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.ID_Proveedor)

                <table class="table table-striped">
                    <tbody>
                        <tr>
                            <th scope="row">Razon Social: </th>
                            <td>
                                @Html.EditorFor(model => model.RazonSocial, new { htmlAttributes = new { @class = "form-control", placeholder = "Razon Social" } })
                                @Html.ValidationMessageFor(model => model.RazonSocial, "", new { @class = "text-danger" })
                            </td>
                        </tr>
                        <tr>
                            <th scope="row">Codigo Tributario</th>
                            <td>
                                @Html.EditorFor(model => model.CodigoTributario, new { htmlAttributes = new { @class = "form-control", placeholder = "Codigo Tributario" } })
                                @Html.ValidationMessageFor(model => model.CodigoTributario, "", new { @class = "text-danger" })
                            </td>
                        </tr>
                    </tbody>
                </table>

                <div class="card-footer text-md-center">
                    <button type="submit" class="btn btn-primary">Guardar cambios</button>
                    @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-secondary ml-2" })
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}