@model Proyecto.Model.Clases.Tiendas
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5 d-flex justify-content-center">
    <div class="card shadow-sm bg-light" style="width:70%">
        <div class="card-header">
            <h4>Tienda</h4>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th style="width: 30%" scope="row">Codigo de la Tienda: </th>
                        <td>
                            @Html.DisplayFor(model => model.Codigo_Tienda)
                        </td>
                    </tr>
                    <tr>
                        <th style="width: 30%" scope="row">Nombre de la Tienda: </th>
                        <td>
                            @Html.DisplayFor(model => model.Nombre_Tienda)
                        </td>
                    </tr>
                    <tr>
                        <th style="width: 30%" scope="row">Descripción de la Tienda:  </th>
                        <td>
                            @Html.DisplayFor(model => model.Direccion_Tienda)
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="card-footer text-md-center mt-3">
                @Html.ActionLink("Editar", "Edit", new { id = Model.ID_Tienda }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Eliminar", "Delete", new { id = Model.ID_Tienda }, new { @class = "btn btn-danger" })
                @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-secondary ml-2" })
            </div>
        </div>
    </div>
</div>